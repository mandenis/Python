name = 'Оформление функции принт'
for i in name:
    #Отступ 4 пробела для каждой инетарции
    print('\t',i)

for i in name:
    #Физуально читабельно
    print(i,end='')

#Геристр букв строки, переменная не меняет значение
name.upper()
name.lower()
#Воводит количство исомых симоволов
name.count()
#Вывод индекста места вхождения второй строки в первую
#Если выводит -1, зачит такая строка не найдена
# if 'KK' in name: .....
name_2 = 'н'
name.find(name_2)
#Замена регистра всех заданных элементов
name.replace('и', 'И')
#Последовательные вызовы функций
name.upper().count('рм'.upper())